export enum UserRole {
  OWNER = 'OWNER',
  STAFF = 'STAFF',
}

export enum ShiftType {
  MORNING = 'MORNING',
  AFTERNOON = 'AFTERNOON',
  NIGHT = 'NIGHT',
  SPLIT = 'SPLIT',
}

export enum ProductStatus {
  OK = 'OK',
  LOW_STOCK = 'LOW_STOCK',
  OUT_OF_STOCK = 'OUT_OF_STOCK',
}
