export enum SocketEvents {
  JOIN_BAR = 'joinBar',
  LEAVE_BAR = 'leaveBar',
  JOINED = 'joined',
  LEFT = 'left',
  PRODUCT_CREATED = 'productCreated',
  PRODUCT_STOCK_CHANGED = 'productStockChanged',
}
